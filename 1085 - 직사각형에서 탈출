#include <iostream>
#include <cmath>
using namespace std;
int x, y, w, h;
int main(void) {
	ios::sync_with_stdio(false);
	cin.tie();
	cin >> x >> y >> w >> h;
	int a = x < abs(w - x) ? x : abs(w - x);
	int b = y < abs(h - y) ? y : abs(h - y);
	cout << (a < b ? a : b) << "\n";
	return 0;
}
